# Temporary stub — will be replaced by dev-1's real implementation
from pathlib import Path


def generate_dashboard(root: Path, project_name: str = "") -> str:
    return "<html><body>Dashboard</body></html>"
