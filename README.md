# yait — Yet Another Issue Tracker

Markdown + git based local issue tracker.

## Install

```bash
pip install -e .
```

## Usage

```bash
yait init          # initialize tracker in current repo
yait new --title "Fix bug"
yait list
yait show 1
```
